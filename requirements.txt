# ============================================================
# STOCK BOT — Dependencies
# Install: pip install -r requirements.txt
# ============================================================

# --- Core ---
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# --- Trading / Market Data (Alpaca is the only required integration) ---
alpaca-py>=0.20
ccxt>=4.2.25              # optional Binance crypto path + free public OHLCV
pandas>=2.2.0
numpy>=2.0.0

# --- Database (SQLite works out of the box; psycopg2 enables Postgres) ---
sqlalchemy>=2.0.23
psycopg2-binary>=2.9.9

# --- Scheduling ---
APScheduler>=3.10.4

# --- Web UI ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- ML (weekly stock scorer) ---
scikit-learn>=1.3.2
xgboost>=2.0.3

# --- Alerts (optional at runtime; bot logs alerts if unconfigured) ---
python-telegram-bot>=20.7

# --- Logging ---
loguru>=0.7.2

# --- Testing ---
pytest>=7.4.3
pytest-asyncio>=0.23.2
httpx>=0.27              # FastAPI TestClient

# --- Utilities ---
requests>=2.31.0
pytz>=2023.3
